# env

This module manages the environment object which stores the current environment
at runtime (e.g. URLs, ports, paths, key vault, db interface, etc..).

It is passed to all modules and is used to access the environment.

It is usually called _e_ or _env_.

