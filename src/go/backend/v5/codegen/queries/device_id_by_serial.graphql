query getDeviceIdBySerial($serial: String) {
    data: devices(
        where: { and: [{ serialNumber: { eq: $serial } }, { serialNumber: { neq: "" } }] }
        take: 1
    ) {
        items {
            id
        }
    }
}