query GetUserIdByEmail($email : String!) {
    users(where: {email:{eq:$email}}) {
        items {
            id
            displayName
            email
            sshPublicKey
            tenantId
            tenant {
                tenantName
            }
        }
    }
}