mutation InitiateUserLogin($email: String!, $sshPublicKey: String!) {
    initiateUserLogin(
        input: {
            email: $email,
            sshPublicKey: $sshPublicKey
        }
    ) {
        challenge
    }
}