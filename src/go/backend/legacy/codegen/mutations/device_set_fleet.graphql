mutation SetDeviceFleet($deviceId: UUID!, $fleetId: UUID){
    updateEdgeDevices(edgeDevices: [{
        id: $deviceId
        fleetId: $fleetId
    }])
    {
        deviceSerial
        deviceName
        deviceDescription
        fleetId
    }
}
