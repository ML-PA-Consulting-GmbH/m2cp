query getUserById($userId: UUID!) {
    user(id: $userId) {
        id
        displayName
        email
        tenantId
        sshPublicKey
        tenant {
            tenantName
        }
    }
}