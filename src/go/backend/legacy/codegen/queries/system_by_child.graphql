query getSystemByChildAssetId($assetId: UUID!) {
  assets(
    where: {
      childAssets: { some: { id: { eq: $assetId } } }
    }
  ) {
    items {
      id
      childAssets {
        id
      }
    }
  }
}

